# Redis 7 — Vision Gateway Cache (256MB, LRU)
maxmemory 256mb
maxmemory-policy allkeys-lru
save ""
appendonly no
maxclients 10000
timeout 0
tcp-keepalive 300
databases 1
protected-mode yes
# hit with: redis-cli --raw info memory
